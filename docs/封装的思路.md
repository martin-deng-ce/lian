
基类的封装：
注意事项：一定要结合项目本身的特点去封装，---接口的风格（规范）
        场景:接口是没有特定的风格，常规化的方式
            一个业务模块粒的不同接口，它们的path都不一样的
        场景2：restful接口风格：一个业务模块，不同的接口path是一样的
        -路径一样
            -查询 GET
            -新增 POST
            -修改 PUT /路径{id} 实际路径/
            -删除 DELETE /路径/{id}